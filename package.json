{
  "name": "change-dark",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "test:e2e:report": "turbo run test:e2e:report",
    "lint": "turbo run lint",
    "check:pre-commit": "pnpm run lint",
    "check:pre-push": "pnpm run build && pnpm run test && pnpm run lint",
    "pack:release": "pnpm run check:pre-push && pnpm --filter @change-dark/chrome run test:e2e:install && pnpm --filter @change-dark/chrome run test:e2e && pnpm --filter @change-dark/chrome run pack",
    "release:chrome": "node scripts/release-chrome.mjs",
    "screenshots:store": "pnpm --filter @change-dark/chrome run screenshots:store",
    "screenshots:site-to-store": "pnpm --filter @change-dark/chrome run screenshots:site-to-store",
    "prepare": "husky && node scripts/hooks/configure-git-hooks.mjs"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.4.4",
    "typescript": "^5.7.3"
  }
}
