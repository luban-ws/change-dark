[package]
name = "dark_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dark_color_utils = { path = "../dark-color-utils" }
wasm-bindgen = "0.2"

[profile.release]
lto = true
opt-level = "z"
