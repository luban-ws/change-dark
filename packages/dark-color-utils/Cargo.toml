[package]
name = "dark_color_utils"
version = "0.1.0"
edition = "2021"
description = "Shared color math for the Selena WASM core (no platform deps)."

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
